# PROBLEM_SLUG — hints

> **Spoilers ahead.** This file names the knowledge point, the approach the constraints
> are asking for, and the problems this one is a cousin of. None of it is needed to
> solve the problem. Read it after your first real attempt, not before — otherwise you
> find out you could have solved it instead of finding out that you can.

## Knowledge point

<!-- The topic or algorithm family this problem is testing, and the standard ways it
     gets implemented. Then the classic traps: the off-by-one everyone writes, the case
     the obvious loop skips, the state that has to be reset between test cases, the
     accumulator that overflows. Say which of those .oa/gen.py actually has a test for
     — a trap the suite covers is one the user will meet, and the rest is trivia. -->

## Why the constraints force it

<!-- The naive approach and its complexity, the arithmetic showing it misses the time
     limit at the stated bound, and the complexity the intended solution reaches. This
     is the reasoning that picked the time limit in the first place; write it down here
     rather than leaving the user to re-derive it from a number in problem.json. -->

## Related problems on LeetCode

<!-- Three to six, closest first. Each: number, title, and a one-line hook saying what
     it shares with this problem — not a restatement of it. Mark premium ones. If this
     problem is a reskin of a LeetCode original, that one goes first, labelled as the
     original.

     English titles and leetcode.com links only, unless the statement is in Chinese or
     the user is working bilingually — then give both titles and both links. The slug
     is identical on the two sites:

     - [210. Course Schedule II / 课程表 II](https://leetcode.com/problems/course-schedule-ii/)
       · [中文](https://leetcode.cn/problems/course-schedule-ii/) — the same topological
       order, but it wants the order itself rather than whether one exists. -->
